services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cloud_costs
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - cloud_cost_data:/var/lib/postgresql/data

volumes:
  cloud_cost_data:
